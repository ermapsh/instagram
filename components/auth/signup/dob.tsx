import React, { memo } from 'react'
import { Text, View } from 'react-native'

function Dob() {
    return (
        <View>
            <Text>dob</Text>
        </View>
    )
}

export default memo(Dob)