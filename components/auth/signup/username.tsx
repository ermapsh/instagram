import React, { memo } from 'react'
import { Text, View } from 'react-native'

function Username() {
    return (
        <View>
            <Text>Username</Text>
        </View>
    )
}
export default memo(Username)